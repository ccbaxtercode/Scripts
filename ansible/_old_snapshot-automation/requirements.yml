---
# Ansible Collection Requirements
# vCenter snapshot yönetimi için gerekli collection'lar

collections:
  - name: community.vmware
    version: ">=3.0.0"
  
  - name: ansible.posix
    version: ">=1.0.0"
  
  - name: community.general
    version: ">=3.0.0"

# Python requirements
dependencies:
  - pyvmomi>=6.7.0
  - requests>=2.25.0
