# ============================================================================
# Service Account for K3s Deployment Jobs
# Used by all Jobs to manage cluster deployment
# ============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-k3s-deployer
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "k3s.labels" . | nindent 4 }}
